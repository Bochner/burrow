apiVersion: hovel.dev/v1alpha1
kind: ModulePackage
metadata:
  name: burrow-transport-prototype
  version: 0.0.0
  moduleType: survey
  summary: Controlled local SSH proof.
  license: Apache-2.0
runtime:
  protocol: jsonrpc-stdio
launch:
  - selector:
      os: linux
      arch: amd64
    command: ["burrow-transport-prototype"]
